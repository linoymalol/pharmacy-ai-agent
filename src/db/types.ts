export type User = {
  id: string;
  name: string;
  // TODO: Add additional fields (locale, eligibility, etc.).
};

export type Medication = {
  id: string;
  name: string;
  // TODO: Add additional fields (dosage, stock, rxRequired, etc.).
};
